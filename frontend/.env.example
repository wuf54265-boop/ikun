# 前端环境变量模板（复制为 .env.local 供本地开发，或参考下方在 Vercel 设置）
#
# NEXT_PUBLIC_* 变量会在 `next build` 时内联进前端包，因此生产环境必须在
# 【构建前】就确定（运行时改环境变量不会生效）。

# 本地开发：指向本地后端
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1

# ─────────────────────────────────────────────────────────────
# Vercel 生产部署：在 Vercel 控制台
#   Project → Settings → Environment Variables 中添加：
#     NEXT_PUBLIC_API_BASE = https://<你的Railway后端>.up.railway.app/api/v1
# （也可直接改本文件里的占位值后提交，但控制台设置更易在拿到 Railway 地址后修改）
# ─────────────────────────────────────────────────────────────
